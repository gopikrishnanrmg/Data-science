library(randomForest)
setwd("~/R projects/Spotify dataset")
data <- read.csv("data.csv")
library(randomForest)
head(data)
summary(data)
data$target = as.factor(data$target)
#data$song_title<-as.character(data$song_title)
smp_size <- floor(0.75 * nrow(data))
set.seed(1)
train_ind <- sample(seq_len(nrow(data)), size = smp_size)
train <- data[train_ind, ]
test <- data[-train_ind, ]
#form<-as.formula(target~acousticness+danceability+energy+instrumentalness+liveness+loudness)
form<-as.formula(target~(.-song_title-artist))
model <- randomForest(form, data = train, family = binomial)
predicted <- predict(model,test)
predicted
dummyvar<-test$target-predicted
plot(model)
cm<-as.matrix(test$target,predicted)
cm
cm<-as.matrix(Actual=test$target,Predicted=predicted)
cm<-as.matrix(table(Actual=test$target,Predicted=predicted))
n=sum(cm)
nc=nrow(cm)
diag=diag(cm)
sum(diag)/n
sum(diag)/sum(s)
plot(test$target,predicted)
model
model
predicted
cm<-as.matrix(Actual=test$target,Predicted=predicted)
cm<-as.matrix(table(Actual=test$target,Predicted=predicted))
sum(diag(cm))/length(test$target)
cm
setwd("~/R projects/Spotify dataset")
data <- read.csv("data.csv")
library(randomForest)
head(data)
summary(data)
data$target = as.factor(data$target)
#data$song_title<-as.character(data$song_title)
smp_size <- floor(0.75 * nrow(data))
set.seed(1)
train_ind <- sample(seq_len(nrow(data)), size = smp_size)
train <- data[train_ind, ]
test <- data[-train_ind, ]
#form<-as.formula(target~acousticness+danceability+energy+instrumentalness+liveness+loudness)
form<-as.formula(target~(.-song_title-artist))
model <- randomForest(form, data = train, family = binomial)
predicted <- predict(model,test)
predicted
#table(test$target, predicted > -0.75)
cm<-as.matrix(table(Actual=test$target,Predicted=predicted))
sum(diag(cm))/length(test$target)
head(test$target)
head(predicted)
summary(test$predicted)
summary(test$target)
summary(predicted)
